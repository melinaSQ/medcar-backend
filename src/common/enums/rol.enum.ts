export enum Rol {
  USER = 'USER', // Rol por defecto para cualquier persona que se registra
  DRIVER = 'DRIVER', // Rol para los conductores de ambulancia
  COMPANY_ADMIN = 'COMPANY_ADMIN', // Rol para el administrador de una empresa
  ADMIN = 'ADMIN', // Rol para ti, el superadministrador del sistema
}